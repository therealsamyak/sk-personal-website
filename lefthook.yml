pre-commit:
  commands:
    lint:
      run: bunx oxlint --fix {staged_files}
      stage_fixed: true
    format:
      run: bunx oxfmt --no-error-on-unmatched-pattern {staged_files}
      stage_fixed: true
